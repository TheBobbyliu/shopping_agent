[pytest]
markers =
    api: smoke/integration tests — no labeled data required
    performance: quality tests — requires fixtures and indexed catalog
    slow: tests that are slow (e.g. full builds); skip with -m "not slow"
testpaths = tests
